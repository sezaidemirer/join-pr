/**
 * Ana sitedeki müşteri / iş birliği marka logoları (Markalarımız, İşbirliklerimiz).
 * Haber kartlarındaki mecra logoları burada değildir; onlar `haber-platform-logos` / kök `public` dosyalarıdır.
 */
export const MARKA_LOGOLARI_PUBLIC_PREFIX = '/marka-logolari';
